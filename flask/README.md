# Flask

This is the flask web application that will run PSDtoSVG with the frontend in the [`frontend`](../frontend/) directory and the PSDtoSVG source code in [`src`](../src/).  Before building this application, ensure you have python and node/npm installed.  This project can be built by running [`build.py`](build.py) which will build dependencies and copy to relevant places.